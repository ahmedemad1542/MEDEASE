/*class ProductsModel {
  final String id;
  final String title;
  final double price;
  final double rating;
  final double stock;

  ProductsModel({
    required this.id,
    required this.title,
    required this.price,
    required this.rating,
    required this.stock,
  });

  factory ProductsModel.fromJson(Map<String, dynamic> json) {
    return ProductsModel(
      id: json['id'],
      title: json['title'],
      price: json['price'],
      rating: json['rating'],
      stock: json['stock'],
    );
  }
}*/
